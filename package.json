{
  "name": "darts",
  "version": "0.1.0",
  "description": "ptite féchette ?",
  "scripts": {
    "lezgo": "tailwindcss -i input.css -o public/output.css --watch",
    "build": "tailwindcss -i input.css -o public/output.css --minify"
  },
  "devDependencies": {
    "daisyui": "^4.6.0",
    "tailwindcss": "^3.4.1"
  }
}
